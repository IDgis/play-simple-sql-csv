play.crypto.secret = "changeme"

play.i18n {
  langs = [ "nl" ]
}

db {
	default.driver=org.postgresql.Driver
	default.url="jdbc:postgresql://gp-test-db:5432/publisher"
	default.username=postgres
	default.password=postgres
}
